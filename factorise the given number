#include<stdio.h>
int main(){
    int i,n,factorise=1;
    scanf("%d",&n);
    for(i=1;i<=n;i++){
        ;
        factorise = factorise * i;
    }
        printf("factorise = %d",factorise);
    return 0;
}
