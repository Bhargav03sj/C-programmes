#include<stdio.h>
#include<limits.h>
int main(){
    int n,i,fib,a=0,b=1,c;
    printf("no. = ");
    scanf(" %d",&n);
    printf("%d %d ",a,b);
    for(i=3;i<=n;i++){
        c=a+b;
        a=b;
        b=c;
        printf("%d ",c);
    }
    return 0;
}
