int main() {
    int i,n,index = -1;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++)
    {
    scanf("%d ",&arr[i]);
    }
    int v;
    scanf("%d",&v);
    for(i=0;i<n;i++)
    {
        if(arr[i]==v)
        {
            index = i;
            break;
        }
    }
        if(index == -1)
        {
        printf("Element not found");
            }
        else 
        {
         printf("Element found at position %d",index+1);   
        }
    return 0;
